# 억양 화살표 기준 v2

> 리듬맵의 화살표를 **언제 무엇으로 찍는가**의 단일 기준. 2026-09-04 Patrick 확정.
> 이 문서가 원본이다 — `chatgpt-note-rules.md` 와 `bot-specs/quiet-wanted-phrase-capture.md`
> 안의 화살표 규칙은 여기서 복사해 넣은 것이다. 고칠 때 세 곳을 같이 고친다.

## 왜 v2 인가

v1 은 「청크·문장 끝에 `→` `↘` `↓` `↗` 중 하나」라고만 했다. **고르는 기준이 없었다.**
그래서 같은 성격의 자리가 노트마다 다르게 찍혔고, 저장된 카드에서 실제로 이런 상태였다
(2026-09-04 실측, 리듬맵 190건):

| 증상 | 건수 |
|---|---|
| `↓` 를 평범한 서술문 끝에 사용 (지시·결정이 아닌데) | 34 |
| 문장 앞 도입부인데 `↘↗` 가 아님 | 58 |
| 나열의 앞 항목인데 `↗` 가 아님 | 52 |
| 평서문 끝인데 `→` 로 끝나거나 화살표가 아예 없음 | 7 |
| Yes/No 의문문인데 `↗` 가 아님 | 3 |

**2026-09-05 추가 발견**: 위 표는 접속사를 `and`/`or`만 다뤘다. Patrick 지적 —
「접속사가 정해진 것만 있는 것도 아니고, 접속사 말고도 다양한 문장·상황에 따라 검토해야 한다」.
1차 교정에서 `but` 반전 예고 앞을 전부 `→`(기본값)로 흘려보낸 게 드러났다 — 실측 33곳 중 27곳.
그래서 남은 334줄(리듬맵·예문·질문·확장예문)을 **문장 의미로 다시 전수 검토**했고, 이번엔
정해진 키워드 목록이 아니라 매 청크 경계의 담화 관계(나열/반전/흐름/여운)를 직접 판단했다.

## 1. 기호 5종

| 기호 | 소리 | 쓰는 자리 |
|---|---|---|
| `→` | 끝을 내리지 않음. 살짝 올리거나 유지, 폭 좁음 | 담담히 이어갈 때. **기본값** |
| `↗` | 마지막 음절을 확실히 올림. 폭 넓음 | 열거·확인·감정·여운 |
| `↘` | 바닥까지 내림 | 여기서 끝 |
| `↓` | `↘` 보다 급하게 뚝 | 못 박는 결론·결정·강한 지시. **아껴 쓴다** |
| `↘↗` | 내렸다가 끝만 다시 튕겨 올림 | 뒤에 더 온다는 신호 |

`→` 와 `↗` 는 반대가 아니라 형제다. 둘 다 「안 끝났다」이고 **폭 차이**일 뿐이다.
그래서 유튜브·영화에서 `→` 자리가 올라가게 들리는 건 정상이다. 대화체일수록 폭이 커진다.

## 2. 공통 원칙 3가지

- 화살표는 **그 청크의 마지막 음절**에서 일어나는 일이다. 다음 단어와 무관하고,
  다음 청크는 항상 중간 높이에서 새로 출발한다.
- `↘`(또는 `↓`)는 **문장당 마지막 1개.** 중간 청크는 절대 바닥까지 내리지 않는다.
- 표기는 **리듬맵 줄에만.** 한글 발음 줄·뜻 줄에는 안 찍는다.

## 3. 판정 순서 — 위에서부터 먼저 걸리는 것으로

### ① 마지막 청크인가?

| 문장 | 화살표 |
|---|---|
| 평서문 · Wh-의문문 · 일반 지시 | `↘` |
| 못 박는 결론·결정·강한 지시 | `↓` |
| Yes/No 의문문 · 되묻기 · 확인 | `↗` |
| 말끝 흐리기 (I guess, kind of, or something) | `↘↗` |

### ② 문장 앞에 떼어놓은 도입부인가? → `↘↗`

콤마가 찍히는 자리가 대부분 여기다.
`This morning,` / `Honestly,` / `To be honest,` / `When I was cleaning at home,` /
`Since we're short on time,` / `If it rains,`

### ③ 나열·선택·반전 예고·여운인가? → `↗`

- 나열 첫 항목 — `fixing A ↗ and replacing B`
- 선택의문 A — `coffee ↗ or tea`
- **반전 예고 — `but`(그 밖에 `though`·`yet`·`however`가 문장 안에서 이어질 때)는 앞이 항상 `↗`.**
  반전이 온다는 신호 자체가 「아직 안 끝났다」를 나열·선택만큼 크게 알린다.
  `I usually get a little stressed every day, ↗ / but today wasn't too BAD. ↘`
  단, `but`이 **새 문장을 여는 것**이면(앞 청크가 진짜 문장 끝) 이 규칙은 안 걸린다 —
  그 경우 앞 청크는 그 문장 나름의 규칙(①)을 그대로 따른다.
- 여운 표현 뒤 — `I'm hoping ↗` / `I'm thinking ↗` / `I was wondering ↗`
- 목록 3개 이상 — `coffee ↗, tea ↗, and juice ↘`

### ④ 나머지 중간 청크 → `→`

**여기는 디폴트가 아니라 「①②③을 다 확인했는데도 안 걸렸다」는 뜻이다.** 접속사 종류로
기계적으로 정하지 말고 의미 관계를 본다 — `that`·`because`·`so`(결과)로 이어지는 앞 절,
한 사건의 단순한 흐름(원인→결과, 순차 서술)이 전형적으로 여기 해당한다.

## 4. `and`·`but`·`or` 갈림길 — ③에서 제일 자주 틀리는 곳

`and` 가 보이면 접속사라고 바로 ④로 넘기지 말 것. **먼저 나열인지 확인한다.**

> 판정법: `and` 앞뒤를 **목록으로 바꿔 말할 수 있나?**

가능하면 나열 → 앞 항목 `↗`

```
I took some pictures ↗ / and posted them on MARketplace. ↘
That means fixing the water pressure issue ↗ / and replacing the GLASS → / before then. ↘
```

불가능하면 한 사건의 흐름 → `→`

```
I was in a RUSH → / and left my phone at home. ↘
```

두 번째는 「서두른 것」과 「두고 온 것」이 목록이 아니라 **원인과 결과**다. 그래서 `→`.

`but`은 `and`처럼 나눠 볼 필요가 없다. **문장 안에서 이어지는 `but`은 거의 항상 `↗`** —
반전 자체가 신호이기 때문이다(위 ③ 참조). 2026-09 서브에이전트 전수 검토에서 실측했다:
저장된 `but` 자리 33곳 중 27곳이 `→`로 잘못 찍혀 있었고, 전부 `↗`로 교정했다.

## 5. 자주 틀리는 자리 — 찍기 전에 이 6개만 확인

- `and` 앞 — 나열이면 `↗`
- **`but` 앞 — 문장 안에서 이어지면 거의 항상 `↗`** (새 문장을 여는 `But`이면 해당 없음)
- 콤마 자리 — 도입부면 `↘↗`
- `I'm hoping` / `I'm thinking` 뒤 — `↗`
- 중간 청크에 `↘` 찍었나 — **문장당 1개만**
- `→→↘` 만 반복됐나 — 위 4개 다시 확인

## 6. 쉐도잉할 때

폭은 그날 텐션대로 가도 된다. **중간에서 끝을 안 내리기만 하면 다 맞는 소리다.**
반대로 중간 청크를 바닥까지 내리면 그 자리에서 문장이 끊긴 것처럼 들려서,
이게 제일 크게 어색해지는 실수다.

`↓` 는 지시·결정 문장에서만 나온다. 상황 설명만 하는 날엔 안 나오는 게 정상이다.

## 7. 앱이 이 표기를 어떻게 쓰는가

| 기호 | 화면 (`rhythmHtml`) | 평문 복원 (`plainFromRhythm`) | TTS SSML (`server.py`) |
|---|---|---|---|
| `→` `↗` | 화살표 그대로 | 삭제 | 삭제 |
| `↘` `↓` | 화살표 그대로 | 마침표 | 마침표 (문장 경계) |
| `↘↗` | **한 덩어리로** 그린다 | 쉼표 | 쉼표 |

`↘↗` 를 `↘` 규칙에 먼저 걸리게 두면 도입부에서 문장이 두 동강 난다
(`As you guys know ↘↗ we had ...` → `As you guys know. we had ...`).
그래서 앱·TTS 양쪽 모두 **`↘↗` 를 `↘` 보다 먼저** 치환한다 (2026-09-04 수정).
