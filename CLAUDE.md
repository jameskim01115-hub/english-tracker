# English Tracker — 작업 규칙

> Patrick 개인 영어 학습 추적기. 이 폴더에서 작업할 때 아래를 지킨다.

## 구조 한눈에

```
Hermes 크론잡 (VPS)
  └─ wanted_phrases.md / b2_vocab_log.md 에 append
       └─ hermes-sync.py (VPS 크론, 매시 :20)
            └─ Firestore: english-tracker-cea9f
                 └─ index.html (GitHub Pages)
```

앱은 단일 파일 `index.html`이다. 빌드 과정이 없다.

## 화면 구조 — 하단 탭 5개

| 탭 | 내용 |
|---|---|
| 오늘 | **달력(이번 주, 접힘)** · 습관 10칸(5×2줄) · 배울 표현(5개) · 설정 |
| 복습 | 오늘 복습 큐 |
| 게임 | 5종 라운드 퀴즈 |
| 발음 | 아무 문장이나 입력 → 원어민 음성 7명 · 속도 5단계 (2026-08-15 추가, 08-19 음성 개편) |
| 표현 | 전체 목록 + 검색 + 필터 + 직접 추가 + 회화 노트 가져오기 + **누적 · 단계별 현황** |

Patrick 요청으로 정해진 것들이라 임의로 되돌리지 말 것:

- **달력은 오늘 탭 맨 위에 있어야 한다.** 기록 전용 탭은 없앴고 누적·설정은 오늘 탭 하단으로 합쳤다.
- **습관 10개는 컴팩트한 아이콘 한 줄 5칸 × 2줄**(`.r-item` / `.r-lb`, `.routines` 는 5열 그리드)이다.
  예전 세로 카드 목록은 자리를 너무 먹는다. 칸 크기는 5개 시절과 같다 — 줄만 늘렸다 (2026-09-02).
  앞줄이 영어(팟캐스트·오늘 표현·복습·말하기 + 스트레칭), 뒷줄이 몸·생활이다.
  체크할 때의 파편 효과·진동은 성취감 장치라 유지한다 (효과음은 Patrick 요청으로 제거됨).
  **`ROUTINES[].k` 는 Firestore 에 그대로 쌓인다 — 이름을 바꾸면 그 키의 과거 기록이 사라진다.**
  합치거나 갈아탈 때는 `legacy: ["옛키"]` 로 승계할 것. 그리고 **루틴 개수를 바꾸면
  과거 완주가 강등되므로** `dayStatus()` 가 그 날 저장해 둔 `total` 로 판정한다.
- **망각 곡선 설명 표를 앱에 넣지 않는다.** 한 번 넣었다가 불필요하다고 뺐다. 규칙은 이 문서에만 둔다.
- **달력은 기본이 이번 주 한 줄**(`calExpanded = false`). 탭하면 그 달 전체가 펼쳐진다.
  전체 달력이 첫 화면을 다 먹으면 정작 할 일이 안 보인다 (접힘 197px / 펼침 352px).
- **동기부여 한 줄(명언)을 다시 넣지 말 것.** 넣었다가 첫 화면이 길다고 제거했다. `QUOTES` 배열도 지웠다.
- **「배울 표현」 기본 노출은 5개**(`newShown = 5`). 15개를 다 펼쳐놓으면 부담만 되고
  아래의 누적·설정에 도달하기 어렵다. 「더 보기」로 10개씩 늘린다.
- 접힌 카드의 긴 문장은 4줄까지만 보인다(`-webkit-line-clamp`). 49단어짜리 문장 하나가
  카드로 화면을 다 먹던 것을 막는 것. 카드를 열면 전체가 보인다.
- 이 조정으로 오늘 탭 높이가 2,539px → 1,526px 로 줄었다. **다시 늘리지 말 것.**

## 학습 흐름 — `studied` 플래그가 관문이다

봇이 배달한 표현은 **바로 복습 큐에 들어가지 않는다.** 「오늘」 탭에서 「학습 완료」를 눌러야
`studied: true`가 붙고 그때부터 복습 주기가 돈다. 안 본 표현이 복습을 막던 문제를 막기 위한 것이다.

- 레거시 판정: `studied === true || stage > 0` (필드 없는 옛 문서 호환)
- **「표현 직접 추가」만 이 관문을 건너뛴다 — 의도된 것이다 (2026-08-17 Patrick 확정).**
  저장 시 `studied: true` + `nextReview = 내일` 이 붙어 **「배울 표현」에 안 나오고 바로 복습 큐로 간다.**
  직접 타이핑한 표현은 이미 아는 것으로 보기 때문이다. Patrick이 "오늘 탭에 왜 안 나오냐"고 물었고,
  **지금 방식을 유지하기로 골랐다.** 다른 소스와 똑같이 「배울 표현」에 쌓이게 바꾸자고 다시 제안하지 말 것.
  - 저장 토스트가 "저장 완료 — 내일부터 복습에 나옵니다"로 이 동작을 이미 알린다. 문구를 지우지 말 것.
- **「학습 완료」는 「오늘」 탭과 「표현」 탭 양쪽에 있다.** 목록을 보다가 바로 복습에 넣을 수 있어야
  오늘 탭으로 되돌아갈 이유가 없다. 소스(봇 배달·막힌 표현·회화 교정)와 무관하게
  **미학습이면 「학습 완료」**, 졸업이면 「복습 큐에 다시 넣기」가 나온다.
  「삭제」는 그 아래 작은 보조 링크다 — 주 동작이 아니다.
- 「지난 것 넘기기」 버튼 = 오늘 이전 미학습 표현 일괄 처리 (`db.batch()`).
  **전부 내일로 몰지 말 것** — `BACKLOG_SPREAD`(5일)에 나눠 배치한다. 31개를 한 번에 내일로 보내면
  다음 날 복습이 31개가 된다. 확인 문구에 "카드를 하나씩 보지 않고 바로 태우는 방식"임을 명시해 둔다 —
  정상 흐름은 카드별 「학습 완료」이고 이 버튼은 밀린 것 처리용이다.

## 복습 규칙

`INTERVALS = [1, 3, 7, 14, 30, 60]`. stage = 성공한 복습 횟수. stage N 달성 → `INTERVALS[N]`일 뒤 재등장.
stage 6 = 졸업(`nextReview = "9999-12-31"`).

| 버튼 | 동작 |
|---|---|
| 알아 | stage +1 |
| 애매 | stage 유지 · 내일 |
| 몰라 | **stage 0으로 완전 리셋** · 내일 |

기본 방향은 **한→영(인출)**. `revDir` 상태, `localStorage.et_dir`에 저장.
**한→영은 복습 탭에만 적용한다** — 「오늘」 탭의 처음 보는 표현은 영→한으로 익히고 넘어온다 (Patrick 확인함).
복습 큐가 비면 「그래도 연습하기」 버튼이 나온다 (`renderPractice`, mode `practice`) —
카드만 넘겨보는 모드라 단계·복습일을 건드리지 않는다.
한국어가 하나도 없는 카드는 한→영을 포기하고 영→한으로 내려간다 (`promptFor()` 가 `ok:false`).
예전엔 영어 상황 태그를 프롬프트로 썼는데 2026-08-17에 없앴다 — 실데이터 172건 전부
`ko`/`meaning`/`question` 중 하나를 갖고 있어 프롬프트를 잃은 카드는 0건이었다.

## 카드 규격 — 순서를 바꾸지 말 것

뒷면 블록은 **항상 같은 순서**다. 데이터가 없는 블록만 빠지고 순서·라벨은 고정한다.
「기준이 애매하다」는 피드백에서 나온 규칙이라 임의로 늘리거나 섞지 말 것.

```
일반               뜻 → 예문(리듬 + 한국어 뜻) → 발음 → 덩어리 → 이렇게도 말해요(접힘) → 진도 → 액션
회화 교정 (한→영)   질문 → 정답 → 내가 썼던 것 → 덩어리 → 왜 틀렸나(접힘) → 진도 → 액션
회화 교정 (영→한)   질문 → 뜻 → 내가 썼던 것 → 덩어리 → 왜 틀렸나(접힘) → 진도 → 액션
```

한→영은 앞면이 뜻(ko)이라 뒷면에서 뜻을 빼고 정답을 넣는다. 영→한은 그 반대다.

회화 교정은 정답을 앞면(한→영) 또는 정답 블록에 Rhythm Map 표기로 한 번만 보여준다.
예문 블록을 따로 두지 않는다 — 같은 문장이 두 번 나오면 중복이다. 상세는 아래 「회화 복습 노트」 절.

- **「막힌 표현」도 리듬 표기로 보여준다** (2026-08-10 추가). Hermes 가 `wanted_phrases.md` 에
  `- Rhythm:` 줄을 같이 쓰고, `parse_wanted()` 가 그걸 `rhythm` 필드로 넘긴다.
  지시문 원본: VPS `skills/productivity/patrick-english-coaching/references/quiet-wanted-phrase-capture.md`,
  붙여넣기용 사본은 `chatgpt-note-rules.md`.
  - 리듬이 없으면 `rhythm` 필드를 **만들지 않는다.** 빈 문자열을 넣으면 앱이 「리듬 있음」으로
    오인해 빈 정답을 그린다. 2026-08-10 이전 14건은 그래서 평문 그대로다.
  - **동기화는 create-only 라 기존 문서에 리듬이 나중에 붙지 않는다.** 옛 카드를 리듬화하려면
    별도 백필이 필요하다(복습 진도를 지키려면 `rhythm` 필드만 PATCH 할 것).
  - `answerHtml()` 은 소스를 가리지 않는다. 반면 **`parseContext()` 의 지름길은 반드시
    `isReview(e)` 로 판정한다** — `e.rhythm` 으로 갈리면 리듬 붙은 막힌 표현의 상황 태그·chunks
    파싱이 통째로 건너뛰어져 한→영 프롬프트가 사라진다. 실제로 밟을 뻔한 함정이다.
- `context` 필드는 소스마다 의미가 다르다. `parseContext()`가 갈라준다.
  - `hermes-delivered` → 예문 (리듬 마크업 포함 가능)
  - `hermes-wanted` → `"<상황 태그>; reusable chunks: a, b, c."`
- **「이럴 때」(영어 상황 태그)는 전부 제거했다 — 어디에도 다시 넣지 말 것** (2026-08-17 Patrick).
  봇의 `- Context:` 앞부분(`Manila city hall / mayor follow-up / delayed action or no update
  after escalation`)이 그 출처였다. 학습에 도움이 안 되고, 그 자리는 **한국어 뜻**이 대신한다.
  - `parseContext()` 가 **`tags` 를 아예 만들지 않는다.** 앞부분을 파싱 단계에서 버리는 것이
    이 규칙의 유일한 강제 장치다 — 렌더에서만 숨기면 다음 사람이 되살린다.
  - 같이 걷어낸 것: 카드의 「이럴 때」 블록, `promptFor()` 의 「이 상황을 영어로」 폴백,
    게임 문장조립 프롬프트·오답 피드백의 태그 폴백, `.chip.tag`·`.expr.ko.tagmode` CSS.
  - 태그 폴백은 **실측 결과 죽은 코드였다** — 172건 전부 `ko`/`meaning`/`question` 중 하나를
    갖고 있어 프롬프트를 잃은 카드가 0건이었다.
  - `context` 원본 문자열은 그대로 둔다. 「표현」 탭 **검색**은 계속 이 필드를 훑는다(화면 표시는 아니다).
- **한국어 뜻은 `meaningOf(e)` 로만 읽는다.** 담기는 필드가 소스마다 다르다 —
  봇 배달·직접 추가는 `meaning`, 막힌 표현·회화 교정은 `ko` 다 (`hermes-sync.py` 가 그렇게 넣는다).
  화면마다 `e.meaning` 을 직접 보던 탓에 **막힌 표현 카드에는 뜻이 아예 안 나왔고**,
  그 빈자리를 영어 상황 태그가 메우고 있었다 (2026-08-17 Patrick 지적).
  - 카드 뜻 블록만 예외로 `isReview(e)` 를 먼저 걸러낸다 — 회화 교정은 위쪽 `isReview` 블록에서
    이미 뜻을 그리므로, 안 걸면 **두 번 나온다.**
  - 게임 **출제 조건**(`playable`·`buildRound` 의 `meaning`/`expr`/`listen` 유형)은 아직
    `e.meaning` 을 본다. 즉 막힌 표현은 문장조립·빈칸 유형만 나온다. 의도적으로 안 건드렸다 —
    4지선다 오답 보기를 긴 문장으로 만들면 문제가 성립하지 않는다.
- **리듬 마크업 — Patrick의 쉐도잉 표기 규칙이다. 임의로 바꾸지 말 것.**
  | 표기 | 뜻 | 렌더 |
  |---|---|---|
  | `**소문자**` | 일반 강세 | `b.st` — 굵게만 |
  | `**대문자**` / 맨 대문자(`HOME`, `re**VIEW**`) | **문장의 피크** | `b.st.peak` — 굵게 + 배경 하이라이트 |
  | `/` | 청크 나누기 | `i.br` — `|` |
  | `→ ↘ ↓ ↗` | 억양 | `i.ar` |
  - **정답 본문은 보통 굵기(`.expr.rh` 400)여야 한다.** 전체를 굵게 하면 강세가 묻힌다 (Patrick 지적).
  - `capsToStress()`는 **대소문자를 바꾸지 않는다.** 대문자가 곧 피크 표시라 살려야 한다.
    `reVIEW` → `re**VIEW**` 처럼 대문자 구간만 떼어낸다.
  - `rhythmHtml()`의 화살표·청크 치환은 **단일 패스**다. 여러 번 치환하면 이미 넣은 태그를
    다시 잡아 중첩되거나 자리표시자가 남는다 — 실제로 두 번 겪었다.
- **핵심 표현 밑줄**: 5단어 이상 문장에서 chunks 중 **하나만** 밑줄(`keyRange()`).
  여러 개 치면 문장 전체가 덮여 의미가 없다. 봇 chunks는 문장 순서대로라 **먼저 나온 것**을 고른다.
  문장 단어 수 − 2 를 넘는 청크는 제외.
- **힌트**: 첫 글자 골격(`L__ m_ …`) 방식은 Patrick이 거부했다. 실제 영어 조각을 단계로 준다 —
  ① 핵심 표현(=밑줄과 동일) ② 문장 앞 절반. 4단어 이하는 1단계뿐.
- **「내 문장」 입력을 다시 넣지 말 것.** 한 번 넣었다가 Patrick이 부담된다고 해서 뺐다 (표현마다 작문은 시간이 너무 걸린다).
## TTS — 서버 음성이 기본이다

**기본은 VPS의 음성 서버**다. 상세: `tts-server/README.md`. 엔진이 **3개**이고 음성 이름으로 갈린다:

| 엔진 | 음성 | 비고 |
|---|---|---|
| Microsoft Neural | `Emma` 등 | edge-tts 경유(키 없음). Azure 키를 넣으면 SSML 승격 |
| **Kokoro-82M (로컬 ONNX)** | **`Puck`·`Echo`** | 남성 전담. `kokoro-onnx` + espeak-ng, 모델은 `/root/tts/kokoro` 마운트(353MB) |
| ~~Supertonic 3~~ | — | **2026-08-19 완전 제거** (아래) |

**Kokoro는 지연 로딩이다** — 모델이 없거나 패키지가 깨져도 Neural 음성은 계속 동작한다.
`/health` 의 `kokoro.models` 로 모델 존재를, `kokoro.loaded` 로 로딩 여부를 본다.

### Supertonic 제거 (2026-08-19)

**남겨둔 이유가 사라져서 지웠다.** 「edge-tts 가 막히면 폴백」이 이유였는데
**Kokoro 가 그 역할을 대신한다** — 로컬 실행이라 Microsoft 쪽 사정과 무관하다.

- 효과: 컨테이너 메모리 **1.29GB → 34.7MB**, 시작 시 모델 로딩(수 초)도 사라졌다
- 옛 선택값(`srv:F2` 등)이 와도 **죽지 않는다** — 서버가 조용히 `DEFAULT_VOICE`(Emma)로 떨어뜨린다. 실측 확인
- 모델 383MB 는 지우지 않고 이름만 바꿔 뒀다: `/root/tts/assets.removed-20260819`
  문제 없으면 `rm -rf` 해도 된다. 되살리려면 이름을 `assets` 로 돌리고 compose 에 마운트를 복구
- 같이 지운 것: `helper.py`, `TTS_ASSETS` 환경변수, `/assets` 마운트, 앱의 `SRV_VOICES_OLD` optgroup
이유: iOS Safari가 다운로드한 Apple 프리미엄 음성을 웹에 노출하지 않아 아이폰에서 기계음밖에
못 쓰던 문제를 해결하려는 것. 기기와 무관하게 같은 품질이 나온다.

```
https://tts.srv1722311.hstgr.cloud/tts?t=<텍스트>&v=F2&s=0.9
```

- `TTS_SPEED = 0.9`, 기본 음성 `srv:Emma`. **`hermes-sync.py`의 `TTS_VOICES`·`TTS_SPEED`와 값이 같아야 한다** —
  다르면 캐시 키가 어긋나 미리 만든 음성을 못 쓰고 매번 새로 생성한다.
- Patrick이 고른 목소리는 **Emma(여성)·Puck(남성)** 둘. 미리 생성도 두 개 다 돌린다 —
  앱에서 바꿔도 기다림이 없게 하려는 것. 목소리를 바꾸려면 `DEFAULT_SRV_VOICE`와
  `hermes-sync.py`의 `TTS_VOICES`를 같이 고치고 프리워밍을 다시 돌릴 것.
  (이 줄은 2026-08-15까지 `F2`·`M1`로 남아 있던 옛 내용이었다 — 08-19에 정정했다.)
- 서버가 죽거나 오프라인이면 `speakServer()`가 `speakDevice()`로 자동 폴백한다. 소리는 항상 난다.
- 설정 선택값(`localStorage.et_voice`)은 `srv:F2` 형태(서버) 또는 voiceURI(기기) 둘 중 하나다.

### 대문자는 소리로 보낼 때 벗긴다 (`unstressCaps`)

TTS 는 대문자 덩어리를 약어로 본다. `UNWIND` 가 「유엔윈드」(U.N. + wind)로 나왔다 — 실측으로
같은 문장이 대문자일 때 0.44초 더 길다. 우리 표기에서 대문자는 **피크 강세**일 뿐이므로 벗겨야 한다.

- **`SPELL_OUT` 에 있는 것만 대문자를 유지**한다(철자로 읽힘). 나머지 대문자는 전부 소문자로.
  볼드 안이든 밖이든 기준은 `SPELL_OUT` 하나뿐이다 — 볼드 유무로 갈리면 `AQUALINK` 같은 이름이
  카드마다 다르게 읽힌다.
- **표기만으로는 구분할 수 없다.** `**UNWIND**`(영어 단어)와 `**CUSA**`(약어)는 화면에서 둘 다
  똑같은 피크다. 그래서 목록이 필요하다. **새 약어가 나오면 `SPELL_OUT` 에 추가할 것.**
- 단어 조각을 건드리면 안 된다 — `de**POS**it` 의 `POS` 는 약어가 아니다. 볼드 앞뒤에 글자가
  붙어 있으면 조각으로 본다.
- 여러 단어 피크(`**SAME HERE**`, `**MOVE FORWARD WITH**`)가 실제로 있다. 공백을 허용해야 잡힌다.
- **`hermes-sync.py` 의 `speech_text()` 와 글자 그대로 같은 규칙이어야 한다.** 어긋나면 캐시 키가
  달라져 미리 만든 음성을 앱이 못 찾는다. 한쪽만 고치지 말 것 — 실제 데이터 120건으로 두 구현을
  대조해 120/120 일치를 확인하고 배포했다.

**기기 음성 폴백 쪽 규칙** (설정에서 직접 고를 때만 쓰임)

- 음성 이름은 OS 언어로 번역돼 온다. 한국어 맥은 `"Ava(프리미엄)"`. 영어 `premium`만 찾으면 못 알아본다 —
  `PREMIUM_RE`·`ENHANCED_RE`·`GOOD_VOICE_RE`에 한글 표기를 넣어뒀으니 지우지 말 것.
- `Samantha`·`Alex`를 우선하도록 짰던 초기 버전이 기계음의 원인이었다. **이름으로 하드코딩하지 말 것.**
- `enVoices()`가 프리미엄/고급/Natural · Google · 미국 영어만 남긴다.
  Daniel(영국)·Karen(호주)·Moira(아일랜드)·Rishi(인도)·Tessa(남아공)는 구형 compact라 뺐다.
- **iOS에는 프리미엄 음성 다운로드를 권하지 말 것.** 받아도 웹에 안 나온다 (Patrick이 실제로 받았다가 헛수고했다).
  길게 설명하던 iOS 안내문은 Patrick 요청으로 제거했다 — 서버 음성이 기본이라 불필요한 노이즈였다.
  지금은 기기 음성에 좋은 게 없을 때만 "서버 음성을 고르면 자연스러워집니다" 한 줄만 띄운다.

**효과음·전역 말하기 속도 설정을 다시 넣지 말 것.** Patrick이 불필요하다고 해서 뺐다.
습관 체크의 파편 효과와 진동은 성취감 장치라 유지한다.

- **예외: 「발음」 탭 안의 속도 선택은 유지한다** (2026-08-15). 그 탭은 발음을 뜯어 듣는 게
  목적이라 속도가 기능의 일부다. **전역 설정으로 끌어올리지 말 것** — 위 규칙은 그대로다.

## 「발음」 탭 (2026-08-15 추가)

아무 영어 문장이나 넣고 원어민 발음을 듣는다. Patrick이 평소 표현을 찾아본 뒤
"실제로 어떻게 발음하나"를 확인하고 싶어서 만들었다.

- **음성 7개** — 여성 **Emma** / 남성 **Puck·Echo**(Kokoro) · **Christopher·Steffan·Andrew·Brian**(Neural).
  Puck·Echo 가 기본 추천이고 나머지 넷은 ★4로 남은 것들이다 (2026-08-19 Patrick 요청으로 추가).
  **Patrick이 블라인드 청취로 직접 채점한 것만 넣는다 — 들어보지 않은 음성을 추가하지 말 것.**
  - **남성이 Kokoro인 이유** (2026-08-19, 3라운드): Patrick 지적 — 모델 음이 자기 음역(105~112Hz)보다
    높으면 따라 하다 발음이 흔들린다. Edge 남성 전량(Andrew·Brian·Christopher·Eric·Steffan·Guy·Roger)을
    실측·청취했으나 **최고 ★4**에 그쳤고 톤이 맞아도 전부 「책 읽는 느낌」이었다. Microsoft가
    `Conversation`으로 분류한 en-US 남성은 Andrew·Brian **둘뿐**이고 나머지는 전부 `News,Novel`이다.
  - **피치를 인위적으로 내리는 건 실패했다.** Andrew −20Hz는 "인위적"이라 제외, Brian −20Hz는 "거칠다".
    **F0를 낮추는 게 답이 아니라 애초에 그 음역인 음성을 찾는 게 답이었다.**
  - Kokoro 미국 남성 9명 전량 비교에서 **am_puck 만 문장·세션이 바뀌어도 ★5를 유지**했다(유일).
    am_echo는 Patrick이 따로 「맘에 든다」고 지정. **남성 ★5는 이번이 처음이다.**
  - **Ava·Ava 2·Jenny·Andrew 2·Brian 2 는 목록에서 뺐다.** 서버는 아직 그 이름을 받아준다 —
    예전 `localStorage.et_voice` 선택이 깨지지 않게 하려는 것.
  - **Andrew·Brian 은 되살렸다** (2026-08-19). Patrick이 처음엔 이름을 지목해 "맘에 안 든다"고 했는데
    **블라인드에서는 둘 다 ★4였다.** 이름을 가리면 평가가 달라진 사례라 기록해 둔다.
- **기본값은 Emma.** 24개 음성 블라인드 비교에서 **유일한 ★5**였다(칩에 ★5 배지).
  Kokoro af_aoede ★4, Supertonic F4 ★4 로 뒤를 이었다. 근거 자료:
  `03_output/tts-비교-20260814/`(비교 페이지 2개 + mp3 40개).
- **속도는 이산 5단계**(0.7/0.8/0.9/**0.9 기본**/1.0/1.1). **연속 슬라이더로 바꾸지 말 것** —
  서버 캐시 키가 `sha1(voice|speed|text)`라 값마다 새로 생성돼 캐시가 무의미해진다.
- 음성·속도를 바꾸면 문장이 있을 때 **즉시 재생**한다. 이 탭의 주 용도가 음성 비교다.
- 최근 12개를 `localStorage.et_say_recent`에 저장하고 탭하면 그때 쓰던 음성·속도로 재생한다.
- 서버가 죽으면 기기 음성으로 폴백한다 — 소리는 항상 난다.
- 입력은 `plain()`을 거친다. 리듬 표기(`**강세**`·`/`·`↘`)를 붙여 넣어도 안전하게 벗겨진다.

**「표현으로 저장」 기능은 아직 없다.** 넣기 전에 Patrick에게 물을 것 —
예전에 「내 문장」 입력을 부담스럽다고 뺀 전례가 있다.

## 회화 복습 노트 가져오기 (ChatGPT Live)

Patrick은 매일 ChatGPT Live로 회화 연습을 하고, 끝나면 복습 노트를 만든다.
**교정된 문장은 본인이 실제로 틀린 것이라 봇이 배달하는 일반 표현보다 학습 가치가 높다.**

표현 탭 → 「💬 회화 복습 노트 가져오기」 → 통째로 붙여넣기 → 미리보기 → 선택 저장.

**텔레그램 봇을 거치지 않는다.** 노트가 4,000자를 넘어 텔레그램 4,096자 한도에 걸리고,
동기화가 매시라 최대 1시간 지연되며, 형식이 고정돼 있어 AI 파싱이 필요 없다.

`parseReviewNote()`가 항목 머리글(`1.1`, `2.1` …)로 자르고 아래 라벨로 필드를 뽑는다.
**라벨 이름이 바뀌면 파싱이 깨진다** — 그래서 저장 전 미리보기를 반드시 거치게 해 뒀다.

```
오늘의 질문 / Patrick's Original Answer / 어색한 부분 / 틀린 문법
Natural Corrected Answer / Rhythm Map / Key Corrections
```

| 노트 | 필드 |
|---|---|
| 오늘의 질문 (영문/한글) | `question` / `questionKo` — **뒷면 첫 블록**(정답 위). 앞면 프롬프트는 `ko` 다 |
| Natural Corrected Answer | `expression`(영문) · `ko`(한글) |
| Patrick's Original Answer | `wrong` |
| 어색한 부분 + Key Corrections 해설 | `why` |
| Rhythm Map | `rhythm` |
| Key Corrections 의 `덩어리 = 뜻` 행 | `chunks` (`|` 구분) |

- **한→영 앞면은 「정답의 한국어(`ko`)」다** (2026-08-10 정정). 예전엔 질문을 앞면에 뒀는데
  (「실제 대화처럼」), 복습 탭에서는 그게 성립하지 않는다 — **질문만 보면 아무 대답이나 만들 수
  있는데 정답은 특정 문장 하나**라, 다르게 말한 뒤 정답을 봐도 비교가 안 된다. Patrick 지적.
  - `promptFor()` 는 `e.ko` 를 최우선으로 본다. 질문 폴백은 `ko` 가 없는 옛 카드용이다.
  - 뒷면 순서: **질문 → 정답 → 내가 썼던 것 → 덩어리 → 왜 틀렸나 → 진도.** 질문이 정답 위다.
  - **영어 질문(`question`)이 없는 카드는 라벨이 「상황」이다.** 그런 카드의 `questionKo` 는
    질문이 아니라 상황 설명이라(11건 중 2건) 「질문」이라고 붙이면 거짓말이 된다.
  - 한→영이면 뜻(ko) 블록을 뒷면에 다시 넣지 않는다 — 앞면과 중복.
  - **영→한 모드는 반대다.** 앞면이 영어 정답이므로 뒷면에 뜻이 있어야 한다. 건드리지 말 것.
- **정답은 Rhythm Map 표기로만 보여준다.** 평문을 따로 또 띄우면 중복이고 카드가 길어진다.
  쉐도잉에 쓰는 건 리듬 쪽이다. `answerHtml()` 참조.
- **정답 자리에 리듬맵만 와도 받는다** (2026-08-12 Patrick이 ChatGPT 지시문을 그렇게 바꿨다).
  `Natural Corrected Answer` 줄에 리듬 표기(`/`·화살표·`**`)가 있고 별도 `Rhythm Map` 섹션이
  없으면 그 줄을 `rhythm` 으로 쓰고 `expression` 은 `plainFromRhythm()` 으로 되살린다.
  안 갈라두면 **`/` 와 `↘` 가 정답 텍스트에 글자로 박혀** 화면에도 나오고 TTS 도 그걸 읽는다.
  - `↘`·`↓` → 마침표, `→`·`↗` → 삭제. 문장 경계가 살아야 TTS 가 끊어 읽는다.
  - **되살린 평문은 완벽하지 않다** — 원래 쉼표 위치와 `NEIL` 같은 고유명사 대소문자는 복구 못 한다
    (`unstressCaps` 가 피크 대문자를 소문자로 내린다). 화면은 리듬을 그리므로 안 보이고,
    발음도 같다. 「표현」 목록·검색·게임에만 영향.
  - **「표현 직접 추가」의 영어 칸도 같은 규칙**이다. 리듬맵을 그대로 넣으면 알아서 갈라진다.
    평문만 넣으면 예전처럼 동작한다.
- **노트 형식은 Patrick이 언제든 바꾼다. 파서가 맞춰야 한다** (2026-08-21).
  가독성 위주로 전면 개편된 노트가 들어왔는데 **항목도 라벨도 하나도 안 잡혀 통째로 실패**했다
  ("노트 형식을 못 찾았다"). Patrick 요구: **"어떻게 넣던 네가 필요한 부분을 찾아서 카드로 넣어라."**
  - **항목 머리글이 `1️⃣ 2️⃣` 키캡 이모지**로 바뀌었다 → `KEYCAP_RE` 로 항목 시작을 인식한다.
    **키캡 노트에서는 「자식 없는 상위 섹션」 규칙을 끈다** — 노트 꼬리의 「내일 할 일 1. 2. 3.」
    번호 목록이 전부 항목으로 둔갑한다.
  - **라벨이 이모지 + 한국어**다(`❌ 내가 한 말`·`✅ 자연스러운 문장`·`🔍 왜 고쳤나`·`💬 입에 붙일 표현`).
    `labelHead()` 가 **앞쪽 장식만** 벗긴다 — 예전 `labelKey()` 는 백틱·볼드를 줄 전체에서 지워서
    라벨과 내용이 한 줄에 붙어 오면 **내용까지 깎았다.**
  - **콜론 없이 `라벨 내용` 한 줄**로 오는 형식을 받는다(`❌ 내가 한 말 \`It's doing well…\``).
    `IMP_LABELS` 는 이제 **접두 패턴**이다(`$` 앵커 없음) — 라벨 뒤 나머지가 곧 내용이다.
  - **`발음` 줄을 새로 담는다**(`pron`). 회화 교정 카드에도 발음 블록이 생겼다.
    **저장 경로(`impSave`)에도 넣어야 한다** — 파서만 고치면 뽑아놓고 화면에 안 나온다.
  - **`뜻` 라벨**이 생겨 `ko` 를 명시적으로 받는다. 없으면 예전처럼 정답 블록의 한글 줄을 쓴다.
  - **질문 줄에 영어와 한국어가 한 줄로 붙어 온다** → `splitEnKo()` 가 첫 한글 글자를 경계로 가른다.
  - **덩어리 구분자가 `=` 에서 `—`·`→` 로** 바뀌었다. 셋 다 받는다.
  - **`unbold()` 가 백틱도 벗긴다.** 새 형식이 영어 조각을 백틱으로 감싸 보내는데 그대로 두면
    「내가 썼던 것」·「왜 고쳤나」에 백틱이 글자로 박힌다. **리듬맵은 이 함수를 안 탄다**(강세 유지).
  - **날짜는 앞 5줄에서 찾는다** — `2026.08.21 (금)` 도, `… 복습 노트 — 2026.08.17` 도 읽는다.
    범위를 안 좁히면 본문 숫자를 날짜로 오인해 카드가 엉뚱한 날짜에 쌓인다.
  - **회귀 검증 필수**: 옛 형식(`1.1`·`Natural Corrected Answer`) 노트도 같이 돌려볼 것.
  - **⚠️ 파일로 테스트한 것과 「실제로 붙여넣은 것」은 다르다.** 붙여넣기 복원기(`htmlToMarked`)가
    **볼드를 `**` 로 바꾸고 백틱을 지운다.** 그래서 라벨이 `**✅ 자연스러운 문장**` 형태로 들어오는데,
    **닫는 `**` 가 내용으로 남으면 그 항목이 통째로 사라진다** — `RHYTHM_MARK` 가 `**` 를 리듬으로
    오인해 `expression` 이 빈 문자열이 되고 `continue` 로 버려진다. 2026-08-21에 실제로 겪었다
    (파일 테스트는 5/5 통과했는데 앱에서는 "항목을 못 찾았습니다").
    → `matchLabel()` 이 라벨 뒤 남은 장식(`*` · 백틱 · `~`)을 벗기고, **글자·숫자가 하나도 없으면
    내용 없음으로 본다.** 테스트할 때는 **볼드가 씌워진 사본**도 같이 돌릴 것.
  - **노트 꼬리가 마지막 항목으로 쓸려 들어간다.** `🔊 오늘의 발음 포인트`·`✅ 내일 할 일` 같은
    섹션은 라벨이 아니라 그대로 두면 **마지막 항목의 「왜 고쳤나」가 10줄이 된다**(2026-08-21 실측).
    → **라벨이 아닌 이모지 섹션 헤더**(`SECTION_HEAD`)를 만나면 수집을 멈춘다.
    **`matchLabel` 뒤에 와야 한다** — 라벨 자체도 이모지로 시작한다(`❌ 내가 한 말`).
  - **「💬 입에 붙일 표현」은 `variants` 로 간다** (표현·뜻·예문 3조각). 예전엔 표현만 `chunks` 에
    남기고 **뜻과 예문을 버렸다.** 카드에서는 **접히는 블록**으로 보여준다 — Patrick이
    「정답·리듬·발음·뜻이 주, 왜 고쳤나·입에 붙일 표현은 접기」로 지정했다 (2026-08-21).
    - 블록 라벨은 소스마다 다르다: 배달 = 「이렇게도 말해요」, 회화 교정 = 「입에 붙일 표현」.
      성격이 다르므로 같은 이름을 쓰지 말 것.
    - **회화 교정에 `variants` 가 있으면 「덩어리로 외우기」 칩을 생략한다** — 같은 조각이 두 번 나온다.
      `chunks` 자체는 계속 채운다(밑줄·힌트·게임이 쓴다). variants 없는 옛 카드는 칩 그대로 — 회귀 없음.
  - **주요 표현이 아닌 항목은 미리보기 목록에서 아예 뺀다**(`thin` = 뜻도 질문도 없음).
    「다음 연습 시작 문장」 같은 게 여기 걸린다 — Patrick: "내가 공부하고 싶은 주요 표현이 아니야".
    - 처음엔 **체크만 풀어뒀는데** Patrick이 "또 항목에 포함되는데 그래서 그냥 저장 안 했어"라고 했다.
      **목록에 남아 있으면 뺀 게 아니다** — 눈에 걸리는 것 자체가 문제였다.
    - 다만 **조용히 사라지면 안 되므로** 요약 줄에 「주요 표현이 아닌 N개 제외」를 표시한다.
      전부 걸러졌으면 「0개 인식」 화면 대신 이유를 토스트로 알린다.
  - **마지막 안전망**: 머리글을 하나도 못 찾았는데 라벨은 보이면, **같은 라벨이 다시 나오는 지점**을
    항목 경계로 쓴다. 카드 하나에 정답·질문이 하나씩이라는 성질만 쓰므로 머리글 형식이 또 바뀌어도 버틴다.
- **정답 라벨은 하나가 아니다 — 동의어 표로 받는다** (2026-08-17). ChatGPT 는 항목 성격에 따라
  제목을 바꿔 쓴다: 교정할 게 없으면 `오늘 연습한 문장`, 확장 연습이면 `오늘 연습한 확장 답변`,
  Patrick 문장이 그대로 좋으면 `Patrick's Sentence`. **`Natural Corrected Answer` 만 보던 탓에
  항목 3개가 통째로 사라졌다**(2.1·6.1·7 — Patrick이 "6번이 왜 빠졌냐"고 지적해 발견).
  새 표기가 나오면 `IMP_LABELS` 에 한 줄 추가한다. **ChatGPT 지시문을 조이지 말 것.**
- **하위 번호가 없는 섹션(`7. 오늘 마지막 표현`)도 항목이다.** 예전엔 `\d+\.\d+` 만 항목 시작으로
  봐서 그런 섹션이 통째로 빠졌다. **자식(`7.1`)이 있는 섹션은 제목이므로 제외**하는데,
  이 조건이 노트 끝의 「오늘 꼭 가져갈 표현 5개」 같은 번호 목록도 같이 걸러준다(그 번호들은 본문에 자식이 있다).
- **정답 자리에 평문과 리듬맵이 둘 다 오면 갈라 쓴다.** 평문→`expression`, 리듬→`rhythm`.
  **중복 저장이 아니다** — 화면에 나오는 건 리듬맵 하나뿐이고(`rhythm`), 평문(`expression`)은
  **TTS·「표현」 탭 목록·검색·게임**이 쓰는 별개의 칸이라 어차피 채워야 한다. 문제는 그 칸을
  어디서 가져오냐인데, **리듬맵에서 되살리면 고유명사가 소문자가 된다**(강세 대문자와 구분 불가):
  `Let's go to BarceLONA ↘` → `barcelona`, `SPAIN` → `spain`. 노트가 평문을 같이 주면 그쪽이 정확하다.
  - 2026-08-17 Patrick이 "둘 다 있으면 중복이니 리듬맵 우선으로 하면 되지 않냐"고 물었고,
    위 근거를 확인한 뒤 **현재 방식을 유지하기로 확정했다.** 리듬맵 단일 소스로 되돌리지 말 것.
  - **단, 평문은 리듬 줄보다 「앞에」 있을 때만 쓴다.** 뒤에 오는 영어 줄은 정답이 아니라
    `포인트`·`추가 예문` 처럼 **라벨로 인식되지 않은 해설 구간이 `fixed` 에 계속 쌓인 것**이다.
    앞뒤를 안 가리고 골랐다가 4.1 정답이 `I have to solve the problem.`(교정 전 원문)으로 바뀌었다.
- **한국어 줄 앞의 `→` 는 벗긴다**(`stripArrow`). 표기 기호지 내용이 아닌데 그대로 저장돼
  카드의 「뜻」과 「질문」이 화살표로 시작하고 있었다.
- **라벨 매칭은 장식에 견뎌야 한다** (`labelKey()` / `matchLabel()`, 2026-08-12).
  ChatGPT 가 섹션 제목을 굵게 하거나 `#`·번호·콜론을 붙이면 정확 일치 정규식이 통째로 빗나가
  **그 섹션이 에러 없이 사라진다.** 실제로 `Rhythm Map` 이 이렇게 빗나가 4건이 평문으로 저장됐다.
  볼드·제목표시·앞번호·불릿·끝콜론을 벗긴 「알맹이」로 비교하고, `라벨: 내용` 한 줄 형태도 받는다.
  - **항목 머리글이 곧 라벨인 노트가 있다** — `1.1 오늘의 질문`. 제목으로 흘려보내면 그 밑 질문이
    어느 섹션에도 안 담겨 사라진다. 머리글도 라벨로 판정하고, 제목은 바로 위 `1. 주제` 줄에서 가져온다.
- **`**` 는 Rhythm Map 에만 쓴다. 나머지 필드는 저장할 때 `unbold()` 로 벗긴다** (2026-08-10 추가).
  ChatGPT 가 정답·질문·덩어리 줄을 **통째로 볼드**로 내보내기 시작했고, 붙여넣기 복원기가
  그걸 충실히 `**...**` 로 되살려 **화면에 `**` 가 글자로 박혔다.** 그날 저장한 10건 전부가 그랬다.
  - 볼드 자체는 잘못이 아니다 — `htmlToMarked()` 는 제 일을 했다. **줄 전체 볼드가 강세가 아닐 뿐이다.**
    그래서 복원기를 건드리지 않고 파서에서 벗긴다. 리듬 표기를 살리는 기능을 깎으면 안 된다.
  - `stripBullet` 은 `*` 하나만 불릿으로 본다. 예전 `/^[*\-•]/` 는 `**덩어리**` 의 첫 `*` 를 먹어
    `*덩어리**` 를 남겼다 — 짝이 깨진 채 저장됐다.
  - 리듬 줄이 통째로 볼드로 와도 `stripOuterBold()` 가 바깥 껍데기만 벗겨 안쪽 강세를 지킨다.
  - 원천 규칙은 `chatgpt-note-rules.md` 에 「볼드는 강세에만 쓴다」로 넣었다.
- **Rhythm Map 은 여러 줄로 와도 전부 이어 붙인다.** 예전엔 `[0]` 만 써서 ChatGPT 가 두 문장을
  두 줄로 보내면 **뒷문장 강세가 통째로 사라졌다.** 한글 줄(해설)만 걸러내고 나머지는 join 한다.
- **단, 리듬이 정답 전체를 덮지 못하면 남은 뒷부분을 평문으로 이어 붙인다** (`rhythmTail()`).
  ChatGPT 가 두 문장짜리 답변에 첫 문장까지만 리듬 표기를 해 보내는 일이 잦다. 그대로 두면
  **화면에는 앞부분만 나오는데 음성(`expression`)은 전체를 읽어** 화면과 소리가 어긋난다 —
  2026-08-10 Patrick이 발견했다. 당시 리듬 있는 11건 중 3건이 잘려 있었다.
  - 자르는 기준은 **단어 개수**다. 리듬이 N단어를 덮었으면 정답의 N+1번째 단어부터가 뒷부분이다.
  - `'90s` → `nineties` 처럼 **단어 수가 같고 표기만 다른 경우는 손대지 않는다.** 발음용
    의도적 차이라 정답 원문으로 덮어쓰면 안 된다 — "그냥 expression 을 쓰면 된다"가 틀린 이유다.
  - 이어 붙인 부분은 크기·색이 같다(`.rh-tail`). 흐리게 만들면 안 읽고 넘어간다.
  - **리듬이 아예 없는 항목도 경고한다** (2026-08-12 추가). 예전엔 `withR` 가 비면 경고 조건이
    전부 거짓이 돼 **아무 말 없이 저장됐다** — 4건이 그렇게 평문으로 들어갔다.
  - **이건 안전장치일 뿐 정상 상태가 아니다.** 평문에는 강세·청크가 없어 문장 전체로 쉐도잉을
    할 수 없다. 그래서 **가져오기 미리보기가 저장 전에 경고**한다(뒷문장을 그대로 보여준다).
    복습하다 발견하면 이미 늦다 — 2026-08-10 Patrick이 그렇게 발견했다.
  - 2026-08-10 기존 7건(오늘 3 + 08-08 3 + 단어 누락 1)은 뒷문장 리듬을 직접 써서 백필했다.
    `rhythmTail()` 이 0을 반환하는지로 검증한다. 리듬은 **단어 수**로 덮는 범위를 따지므로
    ChatGPT 가 `the` 하나를 빠뜨려도 뒷부분이 잘린 것처럼 잡힌다.
- **기호 범례(`굵게 = 강세 · | = 끊어 읽기 …`)를 다시 넣지 말 것.** Patrick이 이미 알고 있어
  매 카드에 반복될 이유가 없다. 「밑줄이 핵심입니다」 안내도 같은 이유로 뺐다.
- **ChatGPT에 붙여넣을 지시문 전문은 `chatgpt-note-rules.md` 에 있다.** 규칙을 고칠 일이 있으면
  그 파일을 고치고 Patrick이 ChatGPT에 다시 붙여넣게 한다. 여기와 그 파일이 어긋나지 않게 할 것.
- **ChatGPT 쪽 Rhythm Map 기준 (2026-08-09 Patrick이 ChatGPT에 고정함)**

  ```
  I just **relaxed** at **HOME** → / and **created** an application / to help me **study** English ↘
  ```

  문장당 피크 1개(**대문자 + 볼드 둘 다**) + 소문자 볼드 2~4개 + `/` 청크 + 억양 화살표.

  - **정답이 두 문장이면 두 문장 다 표기해야 한다** (2026-08-10 추가). 첫 문장만 붙여 보내는
    사고가 실제로 났다 — 화면에서 뒷문장이 사라졌다. 피크는 문장마다 하나씩이므로
    두 문장이면 피크가 2개다. 한 줄로 이어 쓰고 문장이 바뀌어도 줄을 바꾸지 않는다.

  - **피크는 대문자만 쓰면 안 되고 `**HOME**` 처럼 볼드로도 감싸야 한다.** `capsToStress()`는
    문자열에 `**`가 하나라도 있으면 맨 대문자 자동 변환을 끄기 때문이다 —
    `CUSA`·`AQUALINK`·`CRBC` 같은 실제 약어를 강세로 오인하지 않으려는 안전장치라 유지할 것.
  - 소문자 볼드를 2~4개로 제한하는 이유: 전부 강조하면 강세가 의미를 잃는다.
    ChatGPT가 한 문장에 6개를 잡은 적이 있어 명시했다.
  - 이 규칙을 ChatGPT에 고정시키기까지 세 번 걸렸다. **규칙 설명만 고치면 안 되고
    「예시 문장 자체」를 바꿔야 한다** — 설명은 맞게 쓰면서 예시엔 `**`를 안 붙이는 일이 반복됐다.
- 리듬 스타일(`b.st`·`i.ar`·`i.br`)은 **선택자를 좁히지 말 것.** 예문·정답·게임 피드백 어디서든 같게 보여야 한다.
- 가져와도 바로 복습 큐에 안 들어간다. 기존 `studied` 관문을 그대로 타서 「배울 표현」에 쌓인다 —
  하루 6개씩 자동 편입되면 복습이 감당 안 된다.
- 중복 문장은 미리보기에서 자동으로 체크 해제된다.
- **붙여넣기가 굵은 글씨를 자동 복원한다** (`htmlToMarked`). 클립보드에는 평문과 함께 서식(HTML)이
  담기는데 textarea 는 평문만 받아서 `**`가 사라진다 — 실제로 11건이 강세 없이 들어온 적 있다.
  paste 이벤트에서 `text/html`을 읽어 `<b>`/`<strong>`/`font-weight`를 `**`로 되살린다.
  **ChatGPT·Upnote 어디서 어떻게 복사하든 그대로 된다. Patrick에게 복사 방법을 요구하지 말 것.**
  - 살릴 볼드가 없으면 기본 붙여넣기로 넘긴다. 평문 붙여넣기는 방해받지 않는다.
  - 그래도 볼드가 하나도 없으면 미리보기가 경고를 띄운다 — 이중 안전장치라 둘 다 유지할 것.
  - 코드 블록/백틱으로 와도 파서가 껍데기를 벗긴다.
- 저장 직후 `prewarmTTS()`가 F2·M1 음성을 미리 받아둔다 (실패해도 무시).

## 발음(`pron`)·확장(`variants`) — 레슨 원문에서 가져온다 (2026-08-17)

**배경**: 봇은 텔레그램으로 리듬맵·한글발음·확장까지 보내는데, `b2_vocab_log.md` 는
**중복 체크용 3칸(`표현 | 뜻 | 평문예문`)** 이라 나머지가 애초에 안 담긴다. 그래서 트래커에
영어 문장만 들어왔다(Patrick 지적). 원본은 크론 출력 전문에 그대로 남아 있다.

```
/docker/hermes-agent-7jge/data/cron/output/<잡ID>/<날짜>_<시각>.md
```

`hermes-sync.py` 가 날짜로 그 파일을 찾아(`read_lesson`) 표현별로 리듬맵·발음·확장을 뽑는다
(`lesson_units` → `enrich`). 주의점:

- **파일 앞 770여 줄은 프롬프트(스킬 문서)다.** 마지막 `## Response` 뒤부터만 파싱한다 —
  안 자르면 `{리듬맵 표기 영어}` 같은 **템플릿 자리표시자를 데이터로 긁어온다** (실제로 겪음).
- **질문/답변과 확장을 구분한다**(`kind`). 「이렇게도 말해요」에는 **확장 블록만** 넣는다.
  섞으면 대화의 답변이 확장으로 둔갑한다.
- **관사를 무시하고 매칭한다**(`_loose`). 확장 문장은 `issue an OR` → `issue the OR` 처럼
  관사만 바꿔 쓰는 일이 잦다.
- **배달 카드는 리듬맵을 `rhythm` 이 아니라 `context`(예문) 에 넣는다.** 학습 단위가
  `eat out` 같은 덩어리지 문장이 아니기 때문 — `rhythm` 에 넣으면 앞면이 덩어리 대신
  문장으로 바뀌고 예문이 같은 문장을 평문으로 또 보여준다. **막힌 표현(wanted)은 반대다**
  (문장 자체가 학습 단위라 `rhythm` 이 맞다).
- `wanted_phrases.md` 의 `- Pronunciation:` · `- Examples:` 도 파싱한다. 봇이 2026-08 중순부터
  쓰고 있었는데 파서가 몰라 버려지고 있었다.
- `variants` 는 **JSON 문자열**이다(`[{en,pron,ko}]`). 리듬맵에 `/`·`|` 가 들어가 구분자를 못 쓴다.
  앱은 `variantsOf()` 로 읽고, 파싱 실패하면 빈 배열이라 블록이 안 그려진다.

### 예문의 한국어 뜻 (`contextKo`) — 2026-08-18 추가

배달 카드 예문이 **영어와 발음만 나오고 뜻이 없었다** (Patrick 지적). 봇이 안 보낸 게 아니다 —
레슨 원문에 이렇게 3줄로 들어 있고 `lesson_units()` 도 이미 `ko` 로 뽑고 있었는데,
`enrich()` 가 **마지막 `return` 에서 그 값을 버리고 있었다.**

```
We may need to **make** a billing ad**JUST**ment → / if the **CUSA** charge was cal**CU**lated wrong ↘
위 메이 니-d 투 **메익** 어 빌링 어**저s트**먼ㅌ → / 이f 더 **씨유에스에이** 차r지 워z 캘**큘**레이리d 롱 ↘
CUSA 비용 계산이 잘못됐으면 청구 금액을 조정해야 할 수도 있어요.        ← 이 줄이 버려지고 있었다
```

- **`ko` 필드에 넣지 말 것.** `promptFor()` 가 `ko` 를 먼저 보므로 복습 한→영 앞면이
  **「이 예문 문장 전체를 영작하라」** 로 바뀐다. 이 카드의 학습 단위는
  `make a billing adjustment` 같은 **덩어리**지 문장 전체가 아니다. 그래서 `contextKo` 로 분리한다.
- 카드 위쪽 「뜻」(`meaning`)과 **다른 것이다.** 저건 덩어리의 뜻, 이건 예문 문장의 뜻이다. 합치지 말 것.
- 값이 있을 때만 필드를 만든다. 빈 문자열을 넣으면 앱이 「있음」으로 보고 빈 줄을 그린다.
- 앱은 예문 블록 안, 리듬맵 바로 아래에 `.ex-ko` 로 그린다 (「이렇게도 말해요」의 `.v-ko` 와 같은 규격).
- **못 채우는 카드가 남는다.** 레슨 원문에 그 표현이 들어간 문장이 아예 없으면 데이터가 없다
  (2026-08-18 기준 배달 카드 78개 중 67개만 채워짐). 버그가 아니다.

### 막힌 표현은 봇이 정리해서 넣는다 (2026-08-17)

Patrick이 봇에 던지는 「막힌 표현」은 **형식이 없다** — 한국어만, 영어만, 단어 하나, 조각 전부 온다.
**형식을 맞추는 건 Patrick이 아니라 봇의 일이다**(Patrick 명시). 봇 규격이 그렇게 고쳐져 있다:

```
/docker/hermes-agent-7jge/data/skills/productivity/patrick-english-coaching/references/
  quiet-wanted-phrase-capture.md
```

**저장소 사본: `bot-specs/quiet-wanted-phrase-capture.md`** — `.hermes/sync.sh` 가
`patrick-english-coaching` 스킬은 안 받아오므로 이 사본이 유일한 백업이다. 서버를 고치면 여기도 같이 고칠 것.

봇이 채워야 하는 6줄 (하나라도 빠지면 트래커 카드의 그 블록이 안 나온다):

```md
- Original / - Korean / - Rhythm / - Pronunciation / - Context / - Examples
```

`Pronunciation`(→카드의 「발음」)과 `Examples`(→「이렇게도 말해요」)는 2026-08-17에 **필수로 승격**했다.
그전에는 `Pronunciation` 이 "Patrick이 직접 줬을 때만"이었고 `Examples` 는 규격에 없었다 —
그래서 08-14 이전 항목에 발음이 없다(데이터 자체가 없으니 백필로도 못 채운다).

**봇에게 되묻게 하지 말 것.** "무슨 뜻인가요?" 같은 확인 질문은 Patrick이 형식을 맞추게 만드는 것과 같다.

**막힌 표현에 「이렇게도 말해요」는 없어도 된다 (2026-08-17 Patrick 결정).** 규격에
`- Examples:` 를 넣어뒀지만 봇이 실제로는 안 만든다 — 스킬 파일을 세션 시작 때 읽고
갱신하지 않는 것으로 보인다. **봇 컨테이너를 재시작하지 않기로 했다**(다른 잡 영향 우려).
확장은 배달 카드(8AM·10AM)에만 있으면 충분하고, 막힌 표현은 Patrick이 실제로 하려던
그 문장이 핵심이라 리듬·발음까지면 된다. **재시작을 다시 제안하지 말 것.**

### 봇이 라벨을 바꿔도 파서는 견딘다

봇은 규격을 그대로 따르지 않는다. 2026-08-17에 `## 날짜` 헤더 대신 `---`+`Date:`,
`Original:` 대신 `Preferred expression:` 을 썼다. **내용은 완벽한데 라벨만 달랐다.**
봇을 조이는 대신 `WANTED_LABELS` 동의어 표로 파서가 견디게 했다.

새 라벨이 나오면 **`hermes-sync.py` 의 `WANTED_LABELS` 에 추가**하면 된다. 봇 규격을
고칠 필요 없다.

### 옛 문서 백필

동기화는 create-only라 **기존 문서에는 새 필드가 안 붙는다.**

```bash
ssh hermes "cd /root/english-sync && python3 sync_english_to_firestore.py --backfill"
```

`--backfill` 은 `updateMask` 로 `rhythm`·`pron`·`variants`·`contextKo`·`context` 만 덧쓰고
**stage·nextReview 는 건드리지 않는다.** 평소 크론은 이 모드를 쓰지 않는다.

#### ⚠️ `--days N` 은 범위를 넓히는 게 아니라 **카드를 새로 만든다**

나중에 추가한 필드를 과거 카드에 소급 반영할 때 쓴다. 기본 7일(`BACKFILL_DAYS`)을 넘긴다.

```bash
ssh hermes "python3 /root/english-sync/sync_english_to_firestore.py --backfill --days 90"
```

**함정**: 이건 「기존 문서에 필드를 덧쓰는」 작업만 하지 않는다. `b2_vocab_log.md` 에는
**7일 창에 안 걸려 한 번도 Firestore 에 안 들어간 옛 항목**이 남아 있고, 범위를 넓히는 순간
그것들이 `create_doc` 으로 **새 카드가 되어 「배울 표현」 큐에 쌓인다.**

- 2026-08-18 `--days 90` 실행 → `created=24`. 2026-07-27~30 배달분 24건이 통째로 새로 생겼다.
  Patrick 요청으로 전부 삭제했다 (`cleanup_20260727_30.py` 가 VPS 에 남아 있다 — 날짜만 바꿔 재사용 가능).
- **돌리기 전에 `created` 가 0인지 먼저 확인할 방법이 없다.** 돌린 뒤 출력의 `created=` 를 보고
  0이 아니면 어떤 게 생겼는지 조회해 판단할 것.
- **막힌 표현은 7일에 고정된다** (`cutoff` 와 `cutoff_deliv` 를 분리해 둠). 그쪽은
  `lesson_units_window` 로 기간 전체를 훑는 구조라, 범위를 넓히면 몇 달 전 표현에
  **무관한 레슨의 리듬·발음이 붙는다.** 이 분리를 없애지 말 것.

**감정/톤은 넣지 않는다** — Patrick이 2026-08-17에 명시적으로 뺐다.

## 게임 탭

`GAME_LEN = 10`문항. 표현마다 가능한 유형 중 하나를 뽑는다 (`buildRound`).

| 유형 | 조건 |
|---|---|
| `meaning` 뜻 고르기 · `expr` 영어 고르기 · `listen` 듣고 고르기 | `meaning` 필요 |
| `cloze` 빈칸 채우기 | 예문 안에 표현이 그대로 들어 있어야 함 |
| `order` 문장 조립 | 2~8단어. 긴 문장은 `keyRange()`로 뽑은 **핵심 표현만** 조립 |

`order`는 어순 훈련이라 `kinds`에 두 번 넣어 비중을 두 배로 둔다.
4지선다 오답 보기는 **길이가 비슷한 것**에서 뽑는다 — 길이만 봐도 답이 보이면 문제가 안 된다.

**출제 가중치** (`weightOf`): 미학습 ×2.5, 게임 오답 1회당 ×3, 낮은 단계일수록 높게, 졸업 ×0.2.

**게임 오답 횟수는 `missCount` 다. `wrong` 에 넣지 말 것** (2026-08-10 분리).
`wrong` 은 회화 교정의 「내가 썼던 것」(문장)이라 이름이 겹쳐 있었다. 결과가 둘 다 나빴다 —
회화 카드를 게임에서 틀리면 `"문장" + 1` 로 **Patrick이 실제로 썼던 문장 뒤에 숫자가 붙고**,
숫자가 든 배달 카드는 「표현」 탭 검색이 `.toLowerCase is not a function` 으로 터졌다(실제 4건).
- `missOf(e)` 로만 읽는다. 숫자로 남은 옛 `wrong` 은 그대로 세어준다(마이그레이션 불필요).
- 「내가 썼던 것」 블록은 **문자열일 때만** 그린다.

**SRS와의 경계 (중요).** 게임 정답은 복습 일정을 **앞당기지 않는다.** 게임으로 SRS를 우회하면
간격 반복이 무너진다. `right` 카운트만 올린다. 오답일 때만 `wrong` +1 하고,
이미 학습한 표현이면 `nextReview`를 내일로 당긴다. 미학습 표현의 일정은 건드리지 않는다.

기록은 게임이 끝날 때 `db.batch()`로 한 번에 쓴다. 최고 기록은 `localStorage.et_best`.

## 무엇을 불러올 것인가 (중요 — 2026-08-08)

예전엔 열 때마다 `limit(500)`으로 전부 읽었다. 하루 14건(봇 8.2 실측 + 회화 교정 6)씩 쌓이므로
**약 한 달이면 상한에 닿고, 그 순간부터 오래된 표현이 에러 없이 조용히 화면에서 사라진다.**
읽기 횟수도 같이 늘어(하루 10번 열면 5,000회) 무료 한도의 10%를 먹는다.

지금은 평소에 **「지금 필요한 것」만** 읽는다.

| 쿼리 | 범위 |
|---|---|
| `where("nextReview","<=", today+7).orderBy("nextReview").limit(400)` | 복습 대기·임박 + 미학습(동기화가 nextReview를 과거로 넣는다) |
| `orderBy("learnedDate","desc").limit(120)` | 최근 것 — 오늘 화면용 |

- **졸업(`nextReview = "9999-12-31"`)과 먼 미래 복습은 자동으로 빠진다.** 문자열 비교라 정확히 걸린다.
  삭제하지 않아도 앱이 느려지지 않는 이유다 — **졸업 표현을 지우자는 제안은 이래서 불필요하다.**
- 둘 다 **단일 필드 정렬**이라 복합 색인이 필요 없다. Firestore 콘솔 작업이 필요 없다는 뜻.
- 전체 목록은 **「표현」 탭을 열 때 세션당 한 번만** 읽는다 (`ensureAllLoaded`).
  **로딩으로 목록을 가리지 말 것.** 이미 들고 있는 것으로 먼저 보여주고 뒤에서 채운다 —
  헤더에 `· 동기화 중…`만 뜬다. 한 번 가렸다가 회선이 느릴 때 "불러오는 중"만 계속 돈다는
  지적을 받았다 (2026-08-09).
- **Firestore `get()` 에는 타임아웃이 없다.** 회선이 나쁘면 무한정 매달린다.
  `withTimeout()` 으로 직접 끊는다 — 초기 로드 20초, 전체 목록 15초.
  초기 로드가 8초를 넘기면 "연결이 느립니다" 안내로 바꾼다(`showSlow`).
- 로드에 실패했고 캐시도 없으면 **실패했다고 표시한다.** 그냥 `renderAll()` 하면
  "배울 표현을 전부 처리했습니다"가 떠서 다 끝난 것처럼 보인다.
- 그래서 **누적 통계와 단계별 현황은 「표현」 탭에 둔다.** 코어 로드만으론 수치가 틀리기 때문이다.
  전체 로드 전에는 누적이 `—`로 표시된다. 이 둘을 오늘·복습 탭으로 되돌리지 말 것.

용량은 애초에 문제가 아니었다 — 문서당 836바이트, 5년 26,000개라도 24MB(무료 1GB의 2.5%).
**병목은 저장이 아니라 읽기 횟수였다.**

## 로컬 캐시 — 인증을 기다리지 않는다

익명 로그인(~1s) + Firestore 왕복(~0.6s)이 모바일 회선에서 몇 초로 늘어나 "불러오는 중"만 보였다.
`bootFromCache()`가 `localStorage`(`et_cache_v1`)로 **7ms 만에 먼저 그리고**, 서버 데이터가 오면 갈아끼운다.

- 캐시 크기 17KB 수준. 저장 실패는 무시한다 — 없어도 앱은 동작한다.
- 동기화 중에는 헤더 날짜 뒤에 `· 동기화 중…`이 붙는다.
- **인증 전 쓰기는 `canWrite()`가 막는다.** 캐시 화면만 보고 누른 조작이 조용히 사라지면 안 된다.
  쓰기 진입점(루틴 체크·학습 완료·채점·일괄 처리·직접 추가)에 전부 걸려 있다.
- 상태가 바뀌면 `saveCache()`로 갱신한다.

## 날짜 계산은 UTC로 한다 (중요)

`new Date(iso + "T00:00:00")` + `toISOString()` 조합은 마닐라(UTC+8)에서 **하루씩 밀린다.**
예전 `addDays()`가 이 버그를 갖고 있어 복습 예정일과 스트릭이 하루 어긋나 있었다.
`isoToUTC()`를 거쳐 `setUTCDate()`로만 계산할 것.

## 한국어 원문 (`ko` 필드) — 해결됨 2026-08-08

한→영 복습은 `e.ko`를 프롬프트로 쓴다. 없으면 상황 태그로 대체한다.

- 봇 출력이 두 형식이다. 7월 긴 형식엔 `Korean meaning:`이 있었는데 8월 축약 형식에서 빠졌다.
  `hermes-sync.py`의 `parse_wanted()`가 **두 형식을 모두** 읽고 `ko`를 뽑는다 (19 → 33블록).
- 봇 캡처 스펙에 `- Korean:` 줄을 필수로 못박았다:
  `/docker/hermes-agent-7jge/data/skills/productivity/patrick-english-coaching/references/quiet-wanted-phrase-capture.md`
  (chief-of-staff 쪽 examples 파일 2개도 같이 수정, 전부 `.bak-*` 백업 있음)
- 기존 13건은 `ko`를 직접 채웠다 (updateMask로 `ko`만 PATCH — 학습 기록 보존 확인).

주의: 동기화는 doc id로 멱등이라 **기존 문서는 갱신되지 않는다.** 봇 형식이 또 바뀌어 기존 문서를
고쳐야 하면 별도 PATCH 경로가 필요하다.

## 절대 규칙

**최상위(top-level)에서 실행되는 코드 위로 그 코드가 읽는 `const` 를 올려둘 것.**
`loadVoices()` 가 선언 직후 실행되는데 그 안에서 쓰는 `NOVELTY_VOICE` 가 아래에 있어
**스크립트 전체가 ReferenceError 로 죽고 앱이 빈 껍데기가 된 적이 있다 (2026-09-02).**
화면에는 「불러오는 중」만 영원히 돌고 에러도 안내도 없다.
- **간헐적으로 보인다**: `voices.filter(v => NOVELTY_VOICE.test(...))` 는 목록이 비면
  콜백을 안 돌려 TDZ에 안 닿는다. 첫 호출부터 음성이 들어오는 **맥 웹앱에서만** 죽었고
  아이폰·브라우저는 멀쩡했다. 「한 기기에서만 안 된다」가 곧 환경 탓은 아니다
- 부가 기능(음성 등)의 초기 실행은 `try/catch` 로 감싼다. 앱을 죽이면 안 된다
- head 의 Firebase SDK 3개도 같은 급의 단일 실패점이다 — `onerror` 와 전역 error 핸들러가
  이유를 화면에 적게 해뒀다. **이 안전망을 지우지 말 것**


**M Building Firebase 프로젝트(`m-building-fbe46`)를 여기에 연결하지 않는다.** 2026-08-07에 두 앱이 프로젝트를 공유해 M Building 임차인 데이터가 공개 노출된 사고가 있었다. `index.html`과 `hermes-sync.py` 양쪽에 가드가 있으니 우회하지 말 것.

**이 프로젝트에 민감한 데이터를 넣지 않는다.** 화면 로그인이 없고 익명 인증만 걸려 있어, 앱 소스를 읽고 흉내내는 접근은 막지 못한다. 영어 학습 기록 수준만 담는다.

**로그인 화면을 추가하지 않는다.** Patrick이 개인 도구에 로그인 붙이는 것을 명시적으로 거절했다. 주소로 바로 들어가 쓰는 흐름을 유지한다.

## 배포 — 서브모듈이다

이 폴더는 별도 git 저장소(`jameskim01115-hub/english-tracker`)다. **부모 저장소에서 커밋해도 반영되지 않는다.**

```bash
cd 02_projects/english-tracker
git add -A && git commit -m "..." && git push origin main
```

push하면 GitHub Pages가 자동 빌드한다. 1~2분 걸린다.

**배포할 때마다 `index.html`의 `BUILD` 값을 바꿀 것.** 앱이 서버의 index.html을 캐시 없이 받아
자기 `BUILD`와 비교하고, 다르면 상단에 「새 버전이 있습니다」 배너를 띄운다 (탭하면 쿼리를 붙여 새로고침).
안 바꾸면 배너가 안 뜨고 Patrick이 옛 화면을 계속 보게 된다.

**캐시 주의:** Pages가 `cache-control: max-age=600`을 보내고 **iOS Safari는 그보다 더 오래 들고 있다.**
⌘⇧R이 없는 아이폰에서는 탭을 닫았다 열어도 안 바뀔 때가 있다 — 그때는 주소에 `?v=2` 같은 쿼리를 붙인다.
확인할 때는 `?cb=$RANDOM`을 붙여 curl한다. 캐시된 화면을 보고 "배포 실패"로 오판하지 말 것.
2026-08-08에 이 오판이 두 번 반복돼 BUILD 감지를 넣었다.

배포 확인:

```bash
curl -s "https://jameskim01115-hub.github.io/english-tracker/?cb=$RANDOM" | grep -c "찾는문자열"
```

## hermes-sync.py 는 VPS로 따로 배포해야 한다

git push만으로는 서버에 반영되지 않는다. 수정했으면 반드시:

```bash
scp hermes-sync.py hermes:/root/english-sync/sync_english_to_firestore.py
ssh hermes "/usr/bin/python3 /root/english-sync/sync_english_to_firestore.py"
```

- **API 키에 리퍼러 제한이 걸려 있어 스크립트가 `Referer` 를 보내야 한다** (`APP_REFERER`).
  안 보내면 `403 Requests from referer <empty> are blocked` 로 죽는다 — 2026-08-10 제한을 건 뒤
  크론이 조용히 실패하고 있었고 로그를 열어보고서야 알았다. GCP 콘솔에서 허용 도메인을
  바꾸면 이 상수도 같이 고칠 것.
- 서버: SSH alias `hermes` (Hostinger VPS)
- 크론: `20 * * * *` — 매시 20분
- 로그: `/root/english-sync/sync.log`
- Hermes 데이터: `/docker/hermes-agent-7jge/data/english/`

**동기화는 크론잡이 아니라 파일을 읽는다.** 영어 크론을 수정하거나 새로 만들어도 `wanted_phrases.md`·`b2_vocab_log.md`에 계속 append하기만 하면 자동 반영된다. 크론잡마다 Firestore 코드를 넣지 말 것.

## 로컬 테스트

Firestore 설정이 든 상태로 열면 실제 데이터에 붙는다. 읽기는 안전하지만 쓰기 동작(루틴 체크, 복습 처리)은 실제 기록을 바꾼다. UI만 확인할 때는 더미 설정으로 사본을 만들어 띄운다.

`.claude/launch.json`에 `english-tracker` 항목이 등록돼 있다 (포트 4173).

## Firestore

- 프로젝트: `english-tracker-cea9f`
- 컬렉션: `english_days`, `english_expressions` 두 개뿐. 규칙이 나머지를 전부 거부한다.
- 새 컬렉션을 쓰려면 `firestore.rules`에 추가하고 콘솔에서 게시해야 한다. **게시는 Patrick만 할 수 있다** (콘솔 접근 필요).
- 규칙 게시 시 콘솔 URL은 `/firestore/databases/-default-/security/rules`. `/rules`로 가면 개요로 튕긴다.
- 「게시」 버튼은 편집기 내용을 바꿔야 나타난다. 붙여넣기 전에는 없는 게 정상이다.

## 데이터 백업

`backup/`은 gitignore 대상이라 로컬에만 있다. 스키마를 바꾸거나 대량 수정하기 전에 갱신할 것.

```bash
NEW="AIzaSyBmHEyQPrTGd1dQ6wD_zlzVz7EQLBjsEx8"
T=$(curl -s -X POST "https://identitytoolkit.googleapis.com/v1/accounts:signUp?key=$NEW" \
  -H "Content-Type: application/json" -d '{"returnSecureToken":true}' \
  | python3 -c "import sys,json;print(json.load(sys.stdin)['idToken'])")
curl -s "https://firestore.googleapis.com/v1/projects/english-tracker-cea9f/databases/(default)/documents/english_expressions?pageSize=300" \
  -H "Authorization: Bearer $T" -o backup/export_english_expressions.json
```

## 봇 표기

텔레그램 핸들은 `@PatrickHermesAI_bot`이다. 앱 UI에는 봇 이름을 쓰지 않고 핸들만 표기한다 — `HERMES.md`는 어시스턴트 이름을 "낭만파"라고 하는데 Patrick은 그 이름을 쓰지 않는다고 했다. 확정 전까지 이름 표기를 넣지 말 것.
